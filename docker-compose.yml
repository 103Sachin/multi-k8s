version: "3"
services:
  server:
    image: 103sachin/multi-server
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGUSER=$PGUSER
      - PGHOST=$PGHOST
      - PGDATABASE=$PGDATABASE
      - PGPASSWORD=$PGPASSWORD
      - PGPORT=$PGPORT
  client:
    image: 103sachin/multi-client
    mem_limit: 128m
    hostname: client
  worker:
    image: 103sachin/multi-worker
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    image: 103sachin/multi-nginx
    mem_limit: 128m
    hostname: nginx
    ports:
      - 80:80
