name: Deploy MultiK8s
on:
  push:
    branches:
      - main

env:
  SHA: $(git rev-parse HEAD)

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Test
        run: |-
          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
          docker build -t 103sachin/react-test -f ./client/Dockerfile.dev ./client
          docker run -e CI=true 103sachin/react-test npm test

      - name: Build
        run: |-
          docker build -t 103sachin/multi-client:latest -t 103sachin/multi-client:${{ env.SHA }} -f ./client/Dockerfile ./client
          docker build -t 103sachin/multi-server:latest -t 103sachin/multi-server:${{ env.SHA }} -f ./server/Dockerfile ./server
          docker build -t 103sachin/multi-worker:latest -t 103sachin/multi-worker:${{ env.SHA }} -f ./worker/Dockerfile ./worker

      - name: Push
        run: |-
          docker push 103sachin/multi-client:latest
          docker push 103sachin/multi-server:latest
          docker push 103sachin/multi-worker:latest

          docker push 103sachin/multi-client:${{ env.SHA }}
          docker push 103sachin/multi-server:${{ env.SHA }}
          docker push 103sachin/multi-worker:${{ env.SHA }}

      - name: Apply
        run: |-
          kubectl apply -f k8s
          kubectl set image deployments/server-deployment server=103sachin/multi-server:${{ env.SHA }}
          kubectl set image deployments/client-deployment client=103sachin/multi-client:${{ env.SHA }}
          kubectl set image deployments/worker-deployment worker=103sachin/multi-worker:${{ env.SHA }}
